# هذا المستودع = البطاقة التعريفية فقط

صفحتان HTML ثابتتان تُنشران على GitHub Pages، بلا بناء ولا حزم ولا خادم:

| الملف | ماذا يعرض |
|---|---|
| `index.html` | البطاقة التعريفية الرسمية — روابط واتساب وتيليجرام والمتجر |
| `products.html` | عرض منتجات للاطّلاع فقط — بلا سلّة ولا شراء |

كلاهما مكتوب يدويًا بـHTML وCSS داخل الملف نفسه (خط Tajawal وأيقونات
Font Awesome من CDN). تعديلٌ هنا يعني تحرير الملف مباشرة ثم الدفع —
لا شيء يُبنى ولا شيء يُثبَّت.

---

## ⚠ تطبيق العملاء ولوحة الإدارة **ليسا هنا**

مكانهما مستودعٌ آخر: **`hazembarham/sigma-store`** — مشروع Next.js
كامل فيه واجهة المتجر (`/products`, `/wallet`, `/orders`, `/account`)
ولوحة الإدارة (`/admin`) وقاعدة بيانات Prisma.

فإن كان الطلب عن أيٍّ من هذه — الطلبات، المحفظة، شحن الرصيد، صفحة
المنتجات التفاعلية، الاعتراضات، العملاء، المخزون، الإعدادات، القائمة
الجانبية، الإشعارات، الدعم المباشر — فالكود هناك لا هنا. اربط المستودع
بالجلسة (`add_repo` على `hazembarham/sigma-store`) واعمل عليه.

**العلامة الفارقة:** إن لم تجد في هذا المستودع إلا `index.html`
و`products.html`، فأنت في المكان الخطأ لأي طلب يذكر شاشةً من التطبيق.
